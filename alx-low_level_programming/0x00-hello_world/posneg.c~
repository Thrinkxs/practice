#include <stdio.h>

int main(void)
{
int num;
printf("Enter a number: \n");
scanf("%d", &num);
if (num >= 0)
{
printf("%d is a positive integer", num);
}
else if (num == 0)
{
printf("%d is 0", num);
}
else
{printf("%d is a negative integer", num);
}
}
